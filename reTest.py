import re

# url='https://m.momoshop.com.tw/category.momo?cn=4201500000&page=2&sortType=6&imgSH=itemizedType&brandName=GARMIN&brandNoList=20160808155724727'
# nextKeywordRule='page=\d+'
# nextKeyword='page'
# r = re.compile(nextKeywordRule)
# oldKeyword = r.search(url).group()
# numberRule = re.compile('\d+')
# newKeyword = nextKeyword+str(int(numberRule.search(oldKeyword).group()) + 1)
# print(url.replace(oldKeyword, newKeyword))

a=[]
print(len(a) == 0)
